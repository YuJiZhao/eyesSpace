spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/eyesblog?serverTimezone=UTC
      username: root
      password: 792734338Yu!
  redis:
    database: 0
    host: 192.168.204.128
    port: 6379
    timeout: 0

mybatis-plus:
  type-aliases-package: com.eyes.eyesblog.entity
  mapper-locations: classpath:mapper/*Mapper.xml

# TODO: 传递header参数，设置hystrix的隔离策略
# hystrix:
#   command:
#     default:
#       execution:
#         isolation:
#           strategy: SEMAPHORE