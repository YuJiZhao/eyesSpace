spring:
  cloud:
    nacos:
      config:
        server-addr:
        namespace:
        file-extension: yaml
        shared-configs:
          - dataID: program.yaml
            group: dev
            refresh: true
          - dataID: context.yaml
            group: dev
            refresh: true